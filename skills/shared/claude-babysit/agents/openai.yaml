interface:
  display_name: "Claude Babysit"
  short_description: "Supervise Claude against explicit deliverables"
  default_prompt: "Use $claude-babysit to monitor this Claude session, prioritize all accepted deliverables, recover safely from limits, and produce an evidence-backed delivery ledger."
policy:
  allow_implicit_invocation: false
